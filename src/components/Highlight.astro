---
// Destacado entre HowItWorks y CTAFooter. Reframe del problema antes del signup.
---

<section class="highlight">
  <div class="section-wrap">
    <p class="highlight-text">
      El cuello de botella no es la creatividad.
      <br />
      <span class="italic-accent">Es la coordinación.</span>
    </p>
  </div>
</section>

<style>
  .highlight {
    padding-block: var(--space-24);
  }

  @media (min-width: 1024px) {
    .highlight {
      padding-block: var(--space-30);
    }
  }

  .highlight-text {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    line-height: var(--leading-h2);
    letter-spacing: var(--tracking-h2);
    font-weight: 500;
    color: var(--foreground);
    text-wrap: balance;
    margin: 0;
    max-width: 1100px;
  }
</style>
